import { RevAlphabetsPipe } from './rev-alphabets.pipe';

describe('RevAlphabetsPipe', () => {
  it('create an instance', () => {
    const pipe = new RevAlphabetsPipe();
    expect(pipe).toBeTruthy();
  });
});
